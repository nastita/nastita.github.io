<section
  id='main'
  class='pt-80 pb-40 flex items-center justify-center px-6 relative bg-base-100'
>
  <div class='container mx-auto w-full relative'>
    <!-- Top Row: Picture + Name -->
    <div class='flex items-center gap-8 mb-8'>
      <!-- Profile Picture -->
      <div
        class='w-32 h-32 rounded-full overflow-hidden opacity-0 translate-y-8 transition-all duration-600 ease-out flex-shrink-0'
      >
        <img
          src='/images/ed.png'
          alt='Eduardo Sacco'
          class='w-full h-full object-cover'
        />
      </div>

      <!-- Name - Keep on same line -->
      <div class='flex-1 min-w-0'>
        <h1
          class='text-4xl md:text-5xl lg:text-6xl font-black uppercase text-base-content opacity-0 translate-y-8 transition-all duration-600 delay-100 ease-out tracking-tight leading-none whitespace-nowrap'
          style="font-family: 'Monument Sans'; letter-spacing: -0.02em; -webkit-text-stroke: 1px currentColor;"
        >
          Eduardo Sacco
        </h1>
      </div>
    </div>

    <!-- Title Row -->
    <div class='mb-8'>
      <h2
        class='text-3xl md:text-4xl font-bold text-base-content opacity-0 translate-y-8 transition-all duration-600 delay-200 ease-out'
      >
        Senior Software Engineer
      </h2>
    </div>

    <!-- Bottom Row: Bio + Buttons -->
    <div class='grid md:grid-cols-3 gap-8 items-start'>
      <!-- Bio Content -->
      <div
        class='md:col-span-2 opacity-0 translate-y-8 transition-all duration-600 delay-300 ease-out prose prose-lg text-base-content'
      >
        <p>
          <strong>Hi, I'm Ed!</strong> I am an engineer from Argentina with 4+ years
          proven experience in the software development industry.
        </p>
        <p>
          I create robust and innovative software solutions with a focus on web
          and blockchain technologies.
        </p>
        <p>
          I am a quick learner and thinker who takes ownership of his work and
          leverages team work and expertise to drive impact.
        </p>
      </div>
    </div>
  </div>
  <div
    class='absolute bottom-10 left-1/2 -translate-x-1/2 text-2xl animate-bounce z-1000'
  >
    <i class='fas fa-chevron-down'></i>
  </div>
</section>

<!-- Scroll Indicator -->
<div
  class='absolute bottom-10 left-1/2 -translate-x-1/2 text-2xl animate-bounce z-1000'
>
  <i class='fas fa-chevron-down'></i>
</div>

<!-- Scroll Indicator -->
<div
  class='absolute bottom-10 left-1/2 -translate-x-1/2 text-2xl animate-bounce z-1000'
>
  <i class='fas fa-chevron-down'></i>
</div>

<script>
  // Intersection Observer for animations
  const fadeElements = document.querySelectorAll('[class*="opacity-0"]');
  const fadeObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.remove('opacity-0', 'translate-y-8');
        }
      });
    },
    {
      threshold: 0.1,
    }
  );

  fadeElements.forEach((element) => {
    fadeObserver.observe(element);
  });
</script>
